<div class="contenedor-dashboard">
  <h2 class="titulo-dashboard">🌍 Dashboard de Países</h2>

  <div class="botones-dashboard">
    <button class="btn-agregar" onclick="window.location.href='/paises/agregar'">Agregar País</button>
    <button class="btn-editar" onclick="window.location.href='/paises/editar'">Editar País</button>
    <button class="btn-borrar" onclick="window.location.href='/paises/eliminar'">Borrar País</button>
  </div>

  <div class="tabla-container">
    <table class="tabla-paises">
      <thead>
        <tr>
          <th>N°</th>
          <th>ID</th>
          <th>Nombre</th>
          <th>Capital</th>
          <th>Fronteras</th>
          <th>Índice GINI</th>
          <th>Población</th>
          <th>Área</th>
          <th>Zonas Horarias</th>
          <th>Región</th>
          <th>Subregión</th>
          <th>Creador</th>
        </tr>
      </thead>
      <tbody>
        <% if (paises && paises.length > 0) { %>
          <% paises.forEach((pais, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= pais.id || 'N/A' %></td>
              <td><%= pais.name %></td>
              <td><%= pais.capital %></td>
              <td><%= Array.isArray(pais.borders) ? pais.borders.join(', ') : pais.borders || 'N/A' %></td>
              <td><%= pais.gini || 'No especificado' %></td>
              <td><%= pais.population.toLocaleString() %></td>
              <td><%= pais.area ? pais.area.toLocaleString() + ' km²' : 'N/A' %></td>
              <td><%= Array.isArray(pais.timezones) ? pais.timezones.join(', ') : pais.timezones || 'N/A' %></td>
              <td><%= pais.region || 'N/A' %></td>
              <td><%= pais.subregion || 'N/A' %></td>
              <td><%= pais.creador || 'ISABENSA' %></td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="12" class="sin-datos">⚠️ No hay datos disponibles</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>

  <div class="volver">
    <a href="/" class="btn-volver">← Volver al Inicio</a>
  </div>
</div>
