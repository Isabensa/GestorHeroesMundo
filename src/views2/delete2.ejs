<!DOCTYPE html>
<html>
<head>
  <title>Eliminar País</title>
</head>
<body>
  <h1>Eliminar País</h1>

  <!-- Formulario para buscar un país por ID -->
  <form action="/api/paises/eliminar/buscar" method="POST">
    <label for="idPais">ID del País:</label>
    <input type="text" id="idPais" name="idPais" required>
    <button type="submit">Buscar País</button>
    <button type="button" onclick="window.location.href='/api/paises'">Volver al Dashboard</button>
  </form>

  <% if (pais) { %>
    <h2>Datos del País</h2>
    <p><strong>Nombre:</strong> <%= pais.name %></p>
    <p><strong>Capital:</strong> <%= pais.capital %></p>
    <p><strong>Área:</strong> <%= pais.area %></p>
    <p><strong>Población:</strong> <%= pais.population %></p>
    <p><strong>Fronteras:</strong> <%= pais.borders %></p>
    <p><strong>Zonas Horarias:</strong> <%= pais.timezones %></p>
    <p><strong>Región:</strong> <%= pais.region %></p>
    <p><strong>Subregión:</strong> <%= pais.subregion %></p>

    <!-- Botón para borrar el país -->
    <form action="/api/paises/eliminar/<%= pais.id %>" method="POST">
      <button type="submit" style="background-color: red; color: white;">Borrar País</button>
    </form>
  <% } %>
</body>
</html>
